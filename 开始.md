## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

在项目根目录创建 `.env` 文件，参考 `.env` 模板填入配置：

```env
PORT=3001
NODE_ENV=development

# 数据库（可选，留空则使用内存存储）
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# AI 服务商：aliyun | deepseek | openai
AI_PROVIDER=deepseek

# OpenAI（AI_PROVIDER=openai 时使用）
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4o

# 阿里云百炼（AI_PROVIDER=aliyun 时使用，OpenAI 兼容模式）
ALIYUN_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ALIYUN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
ALIYUN_MODEL=qwen-max

# DeepSeek（AI_PROVIDER=deepseek 时使用，OpenAI 兼容模式）
DEEPSEEK_API_KEY=your_deepseek_api_key
# 一般不修改，默认即官方接口；使用第三方兼容网关时再覆盖
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# 可选模型：deepseek-chat（V3，通用/高性价比）、deepseek-reasoner（R1，推理增强）
DEEPSEEK_MODEL=deepseek-chat

# JWT 密钥（用户认证，务必设置为长随机字符串）
JWT_SECRET=your_jwt_secret_key_here_must_be_long_and_secure
```

> API Key 获取方式：
> - **DeepSeek**：登录 [DeepSeek 开放平台](https://platform.deepseek.com/account/api-keys) 创建
> - **阿里云百炼**：登录 [百炼平台控制台](https://bailian.console.aliyun.com/) 的「API 密钥管理」获取

### 3. 启动开发服务

```bash
npm run dev
```

该命令会通过 `concurrently` 同时启动前后端：

- **前端**：http://localhost:5173 （Vite 开发服务器）
- **后端**：http://localhost:3001 （Express，通过 nodemon 热重载）
- Vite 已配置 `/api` 代理到后端，前端可直接调用 `/api/*`

## 可用脚本

| 命令 | 说明 |
| :--- | :--- |
| `npm run dev` | 同时启动前后端开发服务（推荐） |
| `npm run client:dev` | 仅启动前端（Vite） |
| `npm run server:dev` | 仅启动后端（nodemon + tsx 热重载） |
| `npm run build` | 类型检查并构建生产版本到 `dist/` |
| `npm run preview` | 预览生产构建 |
| `npm run check` | TypeScript 类型检查（不输出文件） |
| `npm run lint` | ESLint 代码检查 |

## 卡牌资源说明

- 牌面图片位于 [public/cards/waite/](file:///e:/Programs/VC%20Programs/AI塔罗占卜师/Tarlot_trae/public/cards/waite)，共 78 张 `.jpg`：
  - 大阿卡那：`00-fool.jpg` ~ `21-world.jpg`
  - 小阿卡那数字牌：`1-of-{wands\|cups\|swords\|pentacles}.jpg` ~ `10-of-*.jpg`
  - 小阿卡那宫廷牌：`page-of-*.jpg`、`knight-of-*.jpg`、`queen-of-*.jpg`、`king-of-*.jpg`
- 牌背为 [public/cards/waite/back.svg](file:///e:/Programs/VC%20Programs/AI塔罗占卜师/Tarlot_trae/public/cards/waite/back.svg)，独立于牌面维护
- 卡牌文件名与 URL 映射逻辑见 [api/data/cardImages.ts](file:///e:/Programs/VC%20Programs/AI塔罗占卜师/Tarlot_trae/api/data/cardImages.ts)

## 部署

项目已配置 [vercel.json](file:///e:/Programs/VC%20Programs/AI塔罗占卜师/Tarlot_trae/vercel.json)：

- `/api/(.*)` 路由到 Vercel Serverless Function（入口 [api/index.ts](file:///e:/Programs/VC%20Programs/AI塔罗占卜师/Tarlot_trae/api/index.ts)）
- 其余路由回退到 `index.html`（SPA history 模式）

部署前请在 Vercel 项目设置中配置好上述环境变量。

## 注意事项

- `.env` 文件包含敏感信息（API Key、JWT 密钥），**请勿提交到版本库**。如需团队协作，请将 `.env` 加入 `.gitignore` 并使用 `.env.example` 共享模板。
- 未配置 Supabase 时，用户与记录数据存储在后端内存中，服务重启后会丢失。
- 未配置 AI API Key 时，解牌功能会自动降级为模拟结果。
